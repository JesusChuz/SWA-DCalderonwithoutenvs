export enum FilterOperator {
  eq = 'eq',
  ne = 'ne',
  startswith = 'startswith',
  endswith = 'endswith',
  contains = 'contains',
  gt = 'gt',
  lt = 'lt',
  ge = 'ge',
  le = 'le',
  in = 'in',
}
